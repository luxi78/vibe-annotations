{
  "name": "vibe-annotations-extension",
  "private": true,
  "version": "2.0.2",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "build:firefox": "wxt build -b firefox",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "postinstall": "wxt prepare",
    "test:unit": "node --test tests/**/*.test.js",
    "test:e2e": "wxt build && playwright test tests/e2e/fixture-outside.spec.js tests/e2e/extension-entry.spec.js tests/e2e/selection-keyboard.spec.js tests/e2e/editing-keyboard.spec.js",
    "test:baseline": "wxt build && playwright test tests/e2e/keyboard-baseline.spec.js",
    "test": "node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "wxt": "^0.20.25"
  }
}
